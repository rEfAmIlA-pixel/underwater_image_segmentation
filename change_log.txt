[2025-04-15]
- 基础模型成功跑通，生成 `best_model.pt` 和 `final_model.pt`。

[2025-04-17]
- 编写 `predict.py`，实现使用训练好的模型对输入图像进行预测，并可视化预测结果。

[2025-04-20]
- 优化 `predict.py`，改进预测结果的可视化效果。
- 模型效果欠佳，新增 `utils.py` 中的类别权重计算函数，并在 `train.py` 中引入计算结果（静态权重），优化交叉熵为加权交叉熵。

[2025-04-21]
- 编写 `metrics.py` 文件，包含了计算交并比 (IoU) 与平均交并比 (mIoU) 的函数，并将其添加到模型训练脚本中。

[2025-04-22]
- 优化计算交并比 (IoU) 与平均交并比 (mIoU) 的函数，封装成类并单独成文件 `iou_meter.py`。
- 编写文件 `config.py`，集中管理模型配置信息。

[2025-4-24]
- 结合上述改进重新训练，发现模型预测结果变得更差。类别权重的问题？ -- 重新编写类别权重函数并应用到模型训练脚本中。
- 优化 `predict.py`，使得预测结果大小与原图一致。
- `utils.py` 文件中加入了 `load_model` 模型加载函数，并应用到 `train.py` 与 `predict.py`。
- `config.py` 文件中加入了 `device` 参数。
- 优化 `iou_meter.py`，解决图示多了一个 epoch 的问题。
- 引入学习率衰减策略。

**********
尝试加入学习率策略√，还有模型早停
骨干部分引入resnet
数据预处理阶段：照片大小也许也可以调整
**********